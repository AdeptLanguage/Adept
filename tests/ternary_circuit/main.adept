
import 'sys/ctime.adept'
import 'sys/cstdio.adept'
import 'sys/cstdlib.adept'

func main {
    srand(time(null))
    printf('%d\n', rand() % 2 == 0 ? one() : two())
}

func one int {
    printf('one\n')
    return 1
}

func two int {
    printf('two\n')
    return 2
}
