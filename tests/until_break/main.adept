
import 'sys/cstdio.adept'

func main(in argc int, in argv **ubyte) int {
    countdown int = 10

    until break {
        printf('counting down... %d\n', countdown)
        if countdown == 0, break else countdown -= 1
    }

    printf('Liftoff!\n')
    return 0
}
